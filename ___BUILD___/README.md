# `___BUILD___` — release build scripts

- **Source scripts** for Windows/Linux packaging and FTP deploy live here (`BUILD_ALL_*.py`, `build_*`, `deploy_release_ftp*.py`, `TEMPLATE_RELEASE.html`).
- Run them via the **launchers in the repository root** (same commands as before, e.g. `python BUILD_ALL_WINDOWS.py`) so the working directory stays the project root.
- **`trash/`** — local backups / obsolete copies only (gitignored). Do not put required scripts there.
